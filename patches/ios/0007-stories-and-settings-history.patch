diff --git a/submodules/ChatListUI/Sources/ChatListControllerNode.swift b/submodules/ChatListUI/Sources/ChatListControllerNode.swift
--- a/submodules/ChatListUI/Sources/ChatListControllerNode.swift
+++ b/submodules/ChatListUI/Sources/ChatListControllerNode.swift
@@ -1274,6 +1274,7 @@
         }
         
         self.nebulaSettingsObservation = NebulaSettingsStore.shared.observe { [weak self] in
+            if !NebulaSettingsStore.shared.showStories { self?.scrollToTopIfStoriesAreExpanded() }
             self?.controller?.requestLayout(transition: .immediate)
         }
         self.mainContainerNode.onFilterSwitch = { [weak self] in
@@ -2464,6 +2465,7 @@
 }
 
 func shouldDisplayStoriesInChatListHeader(storySubscriptions: EngineStorySubscriptions, isHidden: Bool) -> Bool {
+    guard NebulaSettingsStore.shared.showStories else { return false }
     if !storySubscriptions.items.isEmpty {
         return true
     }
diff --git a/submodules/SettingsUI/Sources/Search/SettingsSearchRecentQueries.swift b/submodules/SettingsUI/Sources/Search/SettingsSearchRecentQueries.swift
--- a/submodules/SettingsUI/Sources/Search/SettingsSearchRecentQueries.swift
+++ b/submodules/SettingsUI/Sources/Search/SettingsSearchRecentQueries.swift
@@ -1,4 +1,5 @@
 import Foundation
+import NebulaSettingsContract
 import UIKit
 import TelegramCore
 import SwiftSignalKit
@@ -38,6 +39,7 @@
 }
 
 func addRecentSettingsSearchItem(engine: TelegramEngine, item: AnyHashable) {
+    guard NebulaSettingsStore.shared.settingsSearchHistory else { return }
     guard let id = item.base as? String, let data = id.data(using: .ascii) else {
         return
     }
diff --git a/submodules/SettingsUI/Sources/Search/SettingsSearchItem.swift b/submodules/SettingsUI/Sources/Search/SettingsSearchItem.swift
--- a/submodules/SettingsUI/Sources/Search/SettingsSearchItem.swift
+++ b/submodules/SettingsUI/Sources/Search/SettingsSearchItem.swift
@@ -1,4 +1,5 @@
 import Foundation
+import NebulaSettingsContract
 import UIKit
 import Display
 import AsyncDisplayKit
@@ -384,8 +385,15 @@
             return result
         }
         
-        let recentSearchItems = combineLatest(searchableItems.get(), fixedRecentlySearchedItems)
-        |> map { searchableItems, recentItems -> [SettingsSearchableItem] in
+        let nebulaHistoryEnabled = Signal<Bool, NoError> { subscriber in
+            let store = NebulaSettingsStore.shared
+            let observation = store.observe { subscriber.putNext(store.settingsSearchHistory) }
+            subscriber.putNext(store.settingsSearchHistory)
+            return ActionDisposable { observation.cancel() }
+        }
+        let recentSearchItems = combineLatest(searchableItems.get(), fixedRecentlySearchedItems, nebulaHistoryEnabled)
+        |> map { searchableItems, recentItems, enabled -> [SettingsSearchableItem] in
+            guard enabled else { return [] }
             let searchableItemsMap = searchableItems.reduce([AnyHashable : SettingsSearchableItem]()) { (map, item) -> [AnyHashable: SettingsSearchableItem] in
                 var map = map
                 map[item.id] = item
