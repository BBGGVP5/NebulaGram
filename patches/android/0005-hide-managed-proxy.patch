diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ProxyListActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ProxyListActivity.java
index d222504b..3370f1cb 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ProxyListActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ProxyListActivity.java
@@ -88,6 +88,7 @@ public class ProxyListActivity extends BaseFragment implements NotificationCente
     private boolean useProxySettings;
 
     private int rowCount;
+    private int nebulaManagedInfoRow = -1; // NebulaGram
     @Keep
     private int useProxyRow;
     private int useProxyShadowRow;
@@ -593,6 +594,24 @@ public class ProxyListActivity extends BaseFragment implements NotificationCente
 
     private void updateRows(boolean notify) {
         rowCount = 0;
+        boolean wasNebulaManaged = nebulaManagedInfoRow >= 0;
+        nebulaManagedInfoRow = -1;
+        if (app.nebulagram.nebulalink.NebulaLink.isRoutingThroughTunnel()) {
+            useProxyRow = useProxyShadowRow = rotationRow = rotationTimeoutRow = rotationTimeoutInfoRow = -1;
+            connectionsHeaderRow = proxyStartRow = proxyEndRow = proxyAddRow = proxyShadowRow = -1;
+            deleteAllRow = -1;
+            nebulaManagedInfoRow = rowCount++;
+            proxyList.clear();
+            if (notify && listAdapter != null) {
+                selectedItems.clear();
+                listAdapter.checkActionMode();
+                listAdapter.notifyDataSetChanged();
+            }
+            return;
+        }
+        if (wasNebulaManaged) {
+            useProxySettings = MessagesController.getGlobalMainSettings().getBoolean("proxy_enabled", false);
+        }
         useProxyRow = rowCount++;
         if (useProxySettings && SharedConfig.currentProxy != null && SharedConfig.proxyList.size() > 1 && IS_PROXY_ROTATION_AVAILABLE) {
             rotationRow = rowCount++;
@@ -617,7 +636,11 @@ public class ProxyListActivity extends BaseFragment implements NotificationCente
 
         if (notify) {
             proxyList.clear();
-            proxyList.addAll(SharedConfig.proxyList);
+            for (SharedConfig.ProxyInfo proxy : SharedConfig.proxyList) {
+                if (!app.nebulagram.nebulalink.NebulaLink.isTunnelProxy(proxy)) {
+                    proxyList.add(proxy);
+                }
+            }
 
             boolean checking = false;
             if (!wasCheckedAllList) {
@@ -864,7 +887,9 @@ public class ProxyListActivity extends BaseFragment implements NotificationCente
                 }
                 case VIEW_TYPE_INFO: {
                     TextInfoPrivacyCell cell = (TextInfoPrivacyCell) holder.itemView;
-                    if (position == rotationTimeoutInfoRow) {
+                    if (position == nebulaManagedInfoRow) {
+                        cell.setText(getString(R.string.NebulaProxyManagedHint));
+                    } else if (position == rotationTimeoutInfoRow) {
                         cell.setText(getString(R.string.ProxyRotationTimeoutInfo));
                     }
                     break;
@@ -1006,6 +1031,9 @@ public class ProxyListActivity extends BaseFragment implements NotificationCente
 
         @Override
         public int getItemViewType(int position) {
+            if (position == nebulaManagedInfoRow) {
+                return VIEW_TYPE_INFO;
+            }
             if (position == useProxyShadowRow || position == proxyShadowRow) {
                 return VIEW_TYPE_SHADOW;
             } else if (position == proxyAddRow || position == deleteAllRow) {
