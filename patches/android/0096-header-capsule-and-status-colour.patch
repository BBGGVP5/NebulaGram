diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/ActionBar.java b/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/ActionBar.java
--- a/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/ActionBar.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/ActionBar.java
@@ -954,6 +954,12 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
                 if (menu != null) {
                     animators.add(ObjectAnimator.ofFloat(menu, View.ALPHA, 0));
                 }
+                // РџР»Р°РІР°СЋС‰Р°СЏ С€Р°РїРєР° С‡Р°С‚Р° вЂ” С‚Р°РєРѕР№ Р¶Рµ СЂРµР±С‘РЅРѕРє РїР°РЅРµР»Рё, РєР°Рє Р·Р°РіРѕР»РѕРІРѕРє,
+                // Рё СЂРµР¶РёРј РІС‹РґРµР»РµРЅРёСЏ РµС‘ РЅРёС‡РµРј РЅРµ Р·Р°РєСЂС‹РІР°РµС‚: РѕРЅ СЂРёСЃСѓРµС‚ СЃРІРѕСЋ СЃС‚СЂРѕРєСѓ
+                // РїРѕРІРµСЂС…, Рё В«Р’С‹Р±СЂР°РЅРѕ 1В» Р»РѕР¶РёР»РѕСЃСЊ РїСЂСЏРјРѕ РЅР° РЅР°Р·РІР°РЅРёРµ РєР°РЅР°Р»Р°.
+                if (nebulaChatAvatarContainer != null) {
+                    animators.add(ObjectAnimator.ofFloat(nebulaChatAvatarContainer, View.ALPHA, 0));
+                }
             }
             final int color = actionModeColor == 0 ? actionBarColor : actionModeColor;
             if (color == 0 || glassMode) {
@@ -1055,6 +1061,9 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
                 AndroidUtilities.setLightStatusBar((Activity) getContext(), true);
             }
             actionMode.setVisibility(VISIBLE);
+            if (nebulaChatAvatarContainer != null) {
+                nebulaChatAvatarContainer.setAlpha(0f);
+            }
             if (titleTextView[0] != null) {
                 titleTextView[0].setVisibility(INVISIBLE);
             }
@@ -1121,6 +1130,9 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
         if (menu != null) {
             animators.add(ObjectAnimator.ofFloat(menu, View.ALPHA, 1));
         }
+        if (nebulaChatAvatarContainer != null) {
+            animators.add(ObjectAnimator.ofFloat(nebulaChatAvatarContainer, View.ALPHA, 1));
+        }
         if (actionBarColor == 0 || glassMode) {
             NotificationCenter.getGlobalInstance().postNotificationName(NotificationCenter.needCheckSystemBarColors);
         } else if (ColorUtils.calculateLuminance(actionBarColor) < 0.7f) {
@@ -2444,13 +2456,19 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
             final int widthDefault = rightDefault - leftDefault;
             final int left, right;
             final ChatAvatarContainer centeredNebulaAvatar = nebulaFloatingChatHeader
-                    && actionModeFactor == 0f && searchFactor == 0f ? nebulaChatAvatarContainer : null;
+                    && searchFactor == 0f ? nebulaChatAvatarContainer : null;
             if (centeredNebulaAvatar != null) {
                 // The title, subtitle and image have independent places: a
                 // compact real glass capsule stays centred, while the avatar
                 // remains a reachable round control at the trailing edge.
-                final int width = Math.round(nebulaCapsuleWidth.set(centeredNebulaAvatar.getNebulaCenteredHeaderWidth(), !app.nebulagram.ui.NebulaAppearance.liquidAnimations()));
-                left = app.nebulagram.ui.NebulaChatStyle.headerLeft(getWidth(), width);
+                final int capsule = Math.round(nebulaCapsuleWidth.set(centeredNebulaAvatar.getNebulaCenteredHeaderWidth(), !app.nebulagram.ui.NebulaAppearance.liquidAnimations()));
+                // Р РµР¶РёРј РІС‹РґРµР»РµРЅРёСЏ Р·Р°РЅРёРјР°РµС‚ РІСЃСЋ РїР°РЅРµР»СЊ, Рё РЅР° РІСЂРµРјСЏ РїРµСЂРµС…РѕРґР°
+                // РєР°РїСЃСѓР»Р° С‚СЏРЅРµС‚СЃСЏ Рє РЅРµР№ Рё РѕР±СЂР°С‚РЅРѕ. Р Р°РЅСЊС€Рµ РІРµС‚РєР° РІРєР»СЋС‡Р°Р»Р°СЃСЊ Р»РёС€СЊ
+                // РїСЂРё РЅСѓР»РµРІРѕРј РєРѕСЌС„С„РёС†РёРµРЅС‚Рµ: РІСЃС‘ Р·Р°С‚СѓС…Р°РЅРёРµ РїР°РЅРµР»СЊ СЂРёСЃРѕРІР°Р»Р°СЃСЊ РІРѕ
+                // РІСЃСЋ С€РёСЂРёРЅСѓ Рё РІ РїРѕСЃР»РµРґРЅРёР№ РєР°РґСЂ СЃС…Р»РѕРїС‹РІР°Р»Р°СЃСЊ РІ РєР°РїСЃСѓР»Сѓ вЂ” СЌС‚Рѕ Рё
+                // РІС‹РіР»СЏРґРµР»Рѕ РєР°Рє В«СЂР°Р·РґСѓРІР°РµС‚ С€Р°РїРєСѓ Рё РїРѕС‚РѕРј РѕР±СЂР°С‚РЅРѕ РІ РїРёР»СЋР»СЋВ».
+                final int width = lerp(capsule, widthDefault, actionModeFactor);
+                left = lerp(app.nebulagram.ui.NebulaChatStyle.headerLeft(getWidth(), capsule), leftDefault, actionModeFactor);
                 right = left + width;
                 centeredNebulaAvatar.setTranslationX(0);
                 centeredNebulaAvatar.setPivotX(centeredNebulaAvatar.getMeasuredWidth() / 2f);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java
@@ -278,7 +278,7 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
 
         titleTextView = new SimpleTextConnectedView(context, titleTextLargerCopyView);
         titleTextView.setEllipsizeByGradient(true);
-        titleTextView.setTextColor(getThemedColor(Theme.key_actionBarDefaultTitle));
+        titleTextView.setTextColor(nebulaHeaderColor(Theme.key_actionBarDefaultTitle));
         titleTextView.setTextSize(18);
         titleTextView.setGravity(Gravity.LEFT);
         titleTextView.setTypeface(AndroidUtilities.bold());
@@ -292,7 +292,7 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
             animatedSubtitleTextView = new AnimatedTextView(context, true, true, true);
             animatedSubtitleTextView.setAnimationProperties(.3f, 0, 320, CubicBezierInterpolator.EASE_OUT_QUINT);
             animatedSubtitleTextView.setEllipsizeByGradient(true);
-            animatedSubtitleTextView.setTextColor(getThemedColor(Theme.key_actionBarDefaultSubtitle));
+            animatedSubtitleTextView.setTextColor(nebulaHeaderColor(Theme.key_actionBarDefaultSubtitle));
             animatedSubtitleTextView.setTag(Theme.key_actionBarDefaultSubtitle);
             animatedSubtitleTextView.setTextSize(dp(14));
             animatedSubtitleTextView.setGravity(Gravity.LEFT);
@@ -302,7 +302,7 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
         } else {
             subtitleTextView = new SimpleTextConnectedView(context, subtitleTextLargerCopyView);
             subtitleTextView.setEllipsizeByGradient(true);
-            subtitleTextView.setTextColor(getThemedColor(Theme.key_actionBarDefaultSubtitle));
+            subtitleTextView.setTextColor(nebulaHeaderColor(Theme.key_actionBarDefaultSubtitle));
             subtitleTextView.setTag(Theme.key_actionBarDefaultSubtitle);
             subtitleTextView.setTextSize(14);
             subtitleTextView.setGravity(Gravity.LEFT);
@@ -690,6 +690,24 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
         occupyStatusBar = value;
     }
 
+    /**
+     * Р¦РІРµС‚ РЅР°РґРїРёСЃРё РІ С€Р°РїРєРµ С‡Р°С‚Р°.
+     *
+     * <p>РўРµРјР° РґР°С‘С‚ С†РІРµС‚ РґР»СЏ РЅРµРїСЂРѕР·СЂР°С‡РЅРѕР№ РїР°РЅРµР»Рё; РїРѕРІРµСЂС… СЃС‚РµРєР»Р° РѕРЅ РѕРєР°Р·С‹РІР°РµС‚СЃСЏ
+     * СЃР»РёС€РєРѕРј СЏСЂРєРёРј, Рё {@code NebulaChatColors} РµРіРѕ РїСЂРёРіР»СѓС€Р°РµС‚. РџСЂРёРІРµРґРµРЅРёРµ
+     * РґРµР»Р°Р» С‚РѕР»СЊРєРѕ {@code updateColors}, Р° СЃРѕР·РґР°РЅРёРµ РІРёРґР° Рё РєР°Р¶РґРѕРµ РѕР±РЅРѕРІР»РµРЅРёРµ
+     * РїРѕРґРїРёСЃРё СЃС‚Р°РІРёР»Рё С†РІРµС‚ С‚РµРјС‹ РЅР°РїСЂСЏРјСѓСЋ вЂ” РїСЂРё РІС…РѕРґРµ РІ С‡Р°С‚ СЃС‚Р°С‚СѓСЃ СѓСЃРїРµРІР°Р»
+     * РјРёРіРЅСѓС‚СЊ Р±РµР»С‹Рј Рё Р»РёС€СЊ РїРѕС‚РѕРј СЃС‚Р°РЅРѕРІРёР»СЃСЏ СЃРµСЂС‹Рј.
+     */
+    private int nebulaHeaderColor(int key) {
+        final int color = getThemedColor(key);
+        if (!app.nebulagram.ui.NebulaMenuStyle.enabled()) {
+            return color;
+        }
+        return app.nebulagram.ui.NebulaChatColors.foreground(
+                color, app.nebulagram.ui.NebulaMenuStyle.surface(resourcesProvider));
+    }
+
     public void setTitleColors(int title, int subtitle) {
         titleTextView.setTextColor(title);
         subtitleTextView.setTextColor(subtitle);
@@ -1457,7 +1475,7 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
             if (subtitleTextView != null) {
                 subtitleTextView.setText(newSubtitle);
                 if (overrideSubtitleColor == null) {
-                    subtitleTextView.setTextColor(getThemedColor(lastSubtitleColorKey));
+                    subtitleTextView.setTextColor(nebulaHeaderColor(lastSubtitleColorKey));
                     subtitleTextView.setTag(lastSubtitleColorKey);
                 } else {
                     subtitleTextView.setTextColor(overrideSubtitleColor);
@@ -1465,7 +1483,7 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
             } else {
                 animatedSubtitleTextView.setText(newSubtitle, animated);
                 if (overrideSubtitleColor == null) {
-                    animatedSubtitleTextView.setTextColor(getThemedColor(lastSubtitleColorKey));
+                    animatedSubtitleTextView.setTextColor(nebulaHeaderColor(lastSubtitleColorKey));
                     animatedSubtitleTextView.setTag(lastSubtitleColorKey);
                 } else {
                     animatedSubtitleTextView.setTextColor(overrideSubtitleColor);
@@ -1768,7 +1786,7 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
                     if (overrideSubtitleColor != null) {
                         subtitleTextView.setTextColor(overrideSubtitleColor);
                     } else if (lastSubtitleColorKey >= 0) {
-                        subtitleTextView.setTextColor(getThemedColor(lastSubtitleColorKey));
+                        subtitleTextView.setTextColor(nebulaHeaderColor(lastSubtitleColorKey));
                         subtitleTextView.setTag(lastSubtitleColorKey);
                     }
                 } else if (animatedSubtitleTextView != null) {
@@ -1777,7 +1795,7 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
                     if (overrideSubtitleColor != null) {
                         animatedSubtitleTextView.setTextColor(overrideSubtitleColor);
                     } else if (lastSubtitleColorKey >= 0) {
-                        animatedSubtitleTextView.setTextColor(getThemedColor(lastSubtitleColorKey));
+                        animatedSubtitleTextView.setTextColor(nebulaHeaderColor(lastSubtitleColorKey));
                         animatedSubtitleTextView.setTag(lastSubtitleColorKey);
                     }
                 }
@@ -1910,6 +1928,9 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
         if (subtitleTextView != null) {
             width = Math.max(width, subtitleTextView.getExactWidthIncludeDrawables());
         }
+        if (animatedSubtitleTextView != null) {
+            width = Math.max(width, animatedSubtitleTextView.getDrawable().getAnimateToWidth());
+        }
         if (hasVisibleAvatar()) {
             width += dp(52 + 18);
         } else {
