diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/ActionBar.java b/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/ActionBar.java
index 2c10fdd..e3c8146 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/ActionBar.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/ActionBar.java
@@ -240,7 +240,7 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
             // of placing it inside the title capsule.
             params.leftMargin = dp(8);
             params.rightMargin = dp(8);
-            nebulaChatAvatarContainer.setNebulaCenteredTitle(true);
+            nebulaChatAvatarContainer.setNebulaCenteredTitle(true, nebulaSavedMessagesHeader);
             nebulaChatAvatarContainer.setLayoutParams(params);
         } else {
             restoreNebulaAvatarLayout();
@@ -255,7 +255,7 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
             params.width = nebulaAvatarWidth;
             params.leftMargin = nebulaAvatarLeftMargin;
             params.rightMargin = nebulaAvatarRightMargin;
-            nebulaChatAvatarContainer.setNebulaCenteredTitle(false);
+            nebulaChatAvatarContainer.setNebulaCenteredTitle(false, false);
             nebulaChatAvatarContainer.setLayoutParams(params);
         }
         nebulaAvatarWidth = Integer.MIN_VALUE;
@@ -2274,11 +2274,14 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
     private boolean nebulaFloatingChatHeader;
 
     private boolean nebulaChatMenuHidden;
+    private boolean nebulaSavedMessagesHeader;
 
-    public void setNebulaFloatingChatHeader(boolean enabled, boolean hideMenu) {
-        if (nebulaFloatingChatHeader != enabled || nebulaChatMenuHidden != hideMenu) {
+    public void setNebulaFloatingChatHeader(boolean enabled, boolean hideMenu, boolean savedMessages) {
+        if (nebulaFloatingChatHeader != enabled || nebulaChatMenuHidden != hideMenu
+                || nebulaSavedMessagesHeader != savedMessages) {
             nebulaFloatingChatHeader = enabled;
             nebulaChatMenuHidden = hideMenu;
+            nebulaSavedMessagesHeader = savedMessages;
             if (menu != null) {
                 menu.setVisibility(hideMenu ? GONE : VISIBLE);
             }
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
index c70a4b7..37ce3bd 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
@@ -4303,6 +4303,7 @@ public class ChatActivity extends BaseFragment implements
                 userFull = getMessagesController().getUserFull(currentUser.id);
             }
             headerItem = menu.addItem(chat_menu_options, otherIcon);
+            app.nebulagram.ui.NebulaChatStyle.savedMessagesMenuIcon(headerItem, UserObject.isUserSelf(currentUser));
             headerItem.setSubMenuDelegate(new ActionBarMenuItem.ActionBarSubMenuItemDelegate() {
                 @Override
                 public void onShowSubMenu() {
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java
index 3a32683..0009ef4 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java
@@ -757,11 +757,27 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
 
     private boolean glassMode;
     private boolean nebulaCenteredTitle;
+    private boolean nebulaAvatarHiddenForSavedMessages;
+    private int nebulaAvatarOriginalVisibility = -1;
 
     /** Used only by the optional centred Liquid Glass chat header. */
     public void setNebulaCenteredTitle(boolean enabled) {
-        if (nebulaCenteredTitle == enabled) return;
+        setNebulaCenteredTitle(enabled, false);
+    }
+
+    /** Saved Messages puts its mark into the menu slot, never beside its title. */
+    public void setNebulaCenteredTitle(boolean enabled, boolean hideAvatarForSavedMessages) {
+        boolean hideAvatar = enabled && hideAvatarForSavedMessages;
+        if (nebulaCenteredTitle == enabled && nebulaAvatarHiddenForSavedMessages == hideAvatar) return;
         nebulaCenteredTitle = enabled;
+        if (hideAvatar) {
+            nebulaAvatarOriginalVisibility = avatarImageView.getVisibility();
+            avatarImageView.setVisibility(INVISIBLE);
+        } else if (nebulaAvatarHiddenForSavedMessages) {
+            avatarImageView.setVisibility(nebulaAvatarOriginalVisibility);
+            nebulaAvatarOriginalVisibility = -1;
+        }
+        nebulaAvatarHiddenForSavedMessages = hideAvatar;
         titleTextView.setGravity(enabled ? Gravity.CENTER : Gravity.LEFT);
         if (subtitleTextView != null) subtitleTextView.setGravity(enabled ? Gravity.CENTER : Gravity.LEFT);
         if (animatedSubtitleTextView != null) animatedSubtitleTextView.setGravity(enabled ? Gravity.CENTER : Gravity.LEFT);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatActivityEnterView.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatActivityEnterView.java
index 45fb515d..f77cff65 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatActivityEnterView.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatActivityEnterView.java
@@ -14518,7 +14518,7 @@ public class ChatActivityEnterView extends FrameLayout implements
     @Override
     protected void onLayout(boolean changed, int left, int top, int right, int bottom) {
         super.onLayout(changed, left, top, right, bottom);
-        nebulaComposerStyle.layout(emojiButton, attachButton);
+        nebulaComposerStyle.layout(emojiButton, attachButton, senderSelectView);
         if (botCommandLastPosition != -1 && botCommandsMenuContainer != null) {
             LinearLayoutManager layoutManager = (LinearLayoutManager) botCommandsMenuContainer.listView.getLayoutManager();
             if (layoutManager != null) {
