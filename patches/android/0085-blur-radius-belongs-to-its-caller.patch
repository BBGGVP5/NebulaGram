diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/blur3/source/BlurredBackgroundSourceRenderNode.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/blur3/source/BlurredBackgroundSourceRenderNode.java
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/blur3/source/BlurredBackgroundSourceRenderNode.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/blur3/source/BlurredBackgroundSourceRenderNode.java
@@ -81,7 +81,13 @@
 
     @RequiresApi(api = Build.VERSION_CODES.S)
     private void nebulaSetBlur(float radius, RenderEffect input) {
-        if (app.nebulagram.ui.NebulaGlass.custom()) radius = org.telegram.messenger.AndroidUtilities.dpf2(app.nebulagram.ui.NebulaGlass.blur());
+        // Радиус здесь принадлежит вызывающему. Наша настройка стекла задаёт
+        // размытие наших поверхностей, и они передают его сами; подменять им
+        // чужой радиус значило переписывать каждое размытие в приложении —
+        // сторис, обложки, подложку листов, — и при большом значении ползунка
+        // превращать их в цветное пятно.
+        //
+        // Ограничение адаптивного стекла остаётся: оно только уменьшает.
         if (app.nebulagram.ui.NebulaGlass.reduced()) radius = Math.min(radius, org.telegram.messenger.AndroidUtilities.dpf2(6f));
         radius = Math.max(0f, radius);
         if (nebulaBlurRadius == radius && nebulaBlurInput == input) return;
