diff --git a/TMessagesProj/build.gradle b/TMessagesProj/build.gradle
--- a/TMessagesProj/build.gradle
+++ b/TMessagesProj/build.gradle
@@ -37,6 +37,7 @@ configurations.all {
 }
 
 dependencies {
+    implementation files('libs/nebulalink.aar')  // NebulaGram: NebulaLink core
     implementation 'androidx.core:core:1.16.0'
     implementation 'androidx.palette:palette:1.0.0'
     implementation 'androidx.exifinterface:exifinterface:1.3.6'
@@ -146,6 +147,14 @@ android {
 
         multiDexEnabled true
 
+        // NebulaGram: какие архитектуры компилировать. Нативную сборку ведёт
+        // именно этот модуль, и без фильтра он строит все четыре независимо от
+        // splits в модуле приложения — это и есть основное время сборки.
+        ndk {
+            abiFilters(*((project.findProperty("nebulaAbis")
+                    ?: "armeabi-v7a,arm64-v8a,x86,x86_64").toString().split(",").collect { it.trim() }))
+        }
+
         externalNativeBuild {
             cmake {
                 version '3.22.1'
