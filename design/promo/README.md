# NebulaGuard announcement

`nebulagram-nebulaguard-poster.png` is the corrected poster for the announcement in `docs/nebulaguard-nebulagram-post.md`.

Both Android and iOS are available, as confirmed by the project owner. The bottom line reads: **Доступно на Android и iOS**.

Edited with the built-in ImageGen tool. Final edit prompt: Change only the bottom platform status line from “Android — уже доступен / iOS — в разработке” to “Доступно на Android и iOS”. Center it at the same position, using the same light-blue typography and size. Preserve the composition, background, logo, lighting, all other text and typography.
