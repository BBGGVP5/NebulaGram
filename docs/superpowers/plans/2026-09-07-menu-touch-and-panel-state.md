# Меню и состояние нижней панели

**Цель:** устранить повторные замечания после fa93188, включая дополнения про сообщение и угол закрытия.

**Реализация:** общий материал и движение в overlay, интеграция в Telegram новым патчем 0062. Выполнять в этой задаче.

- [x] Проверять контраст непосредственно перед отрисовкой меню, включая пользовательские ячейки аккаунтов и обновления темы после измерения.
- [x] Добавить небольшую упругость и ограниченную деформацию стеклянной формы при движении пальца; сохранить штатные выбор пункта и прокрутку.
- [x] Запускать общее раскрытие меню сообщения и сохранять его исходную точку для закрытия.
- [x] Сохранять скрытие оригинала выбранного сообщения и группы; размещать меню снизу независимо от размытия.
- [x] Скрывать оформление выключенной панели, убрать два нижних пояснения, плавно менять общую ширину вместе с вкладками.
- [x] Сужать кнопку назад до круга, когда счётчик равен нулю.
- [x] Проверить регрессии, применение патчей и Java-компиляцию. Отправка: коммит по-русски от BBGGVP5, push без ожидания CI; результат фиксируется в итоговом сообщении.

## Дополнения
- [x] NebulaRow: подсветка реальной строки, поиск прокручивает к ней.
- [x] NebulaGlassSettings, NebulaExpand: тумблеры, анимированное раскрытие, прозрачность/размытие/преломление и блики, превью; подключение к материалу меню и EmojiView.
- [x] NebulaHaptics: включение, сила, пробный отклик; папки и меню пользуются одной политикой.
- [x] NebulaDesignFragment: тумблер перед закруглением аватарок.
- [x] FilterTabsView: движение пальцем, упругая линза и выбор при отпускании, направление раскрытия от папки.
- [x] NebulaComposerStyle и ActionBar: редактирование в общем поле и скругление центральной пилюли.
- [ ] NebulaLinkShortcut: настройка видимости, нажатие для подключения, удержание для сервера/пинга, четыре состояния с превью.
- [x] NebulaSettingsFragment: общая подписанная категория ИИ и приложения.

## Проверка продолжения, 2026-09-08

Пункты выше отмечены по реализации и локальным регрессионным проверкам, не по визуальной проверке на устройстве. Восстановлены 14 нативных файлов в патч 0062, вся последовательность из 57 патчей применяется к чистой базе. Все 14 скриптов UI/JVM-регрессий прошли.

Общий `check-java.py` по-прежнему сообщает о wildcard-import в существующем коде; сравнение с HEAD не обнаружило новых предупреждений или повторных локальных объявлений. Решающее подтверждение корректности Java — компиляция Android, а не эта эвристика.

Отдельный невыполненный пункт прежнего расширенного плана — NebulaLinkShortcut; он не входит в восстановленный патч меню/панели. APK, реальные касания и отрисовка на телефоне требуют отдельной проверки.

Финальная компиляция после возврата стандартной иконки видеокружка прошла: `BUILD SUCCESSFUL in 22s`. Тест сначала воспроизводил подмену, затем прошёл для обоих состояний и всех наборов иконок.
